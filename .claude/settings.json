{
  "permissions": {
    "allow": [
      "Bash(next dev)",
      "Bash(next build)",
      "Bash(next lint *)",
      "Bash(next start)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ \"$f\" =~ \\.(ts|tsx|js|jsx|css|md)$ ]] && npx prettier --write \"$f\" 2>/dev/null; } || true"
          }
        ]
      }
    ]
  }
}
