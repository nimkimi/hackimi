// Stub for the `server-only` package so modules that import it can be
// exercised in tests. The real package throws when imported outside a
// React Server Component; here it is an intentional no-op.
export {};
